# Web build for the OpenBNCT workbench — `trunk build` / `trunk serve`
# from this directory produces the browser bundle.
[build]
target = "index.html"
release = true

[serve]
port = 8080
open = false
